# wgpu 29 API Update Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix wgpu 29 API errors in `crates/vorce-render` by applying required descriptor and type changes.

**Architecture:** Systematic update of all RenderPass, RenderPipeline, and PipelineLayout descriptors, along with Sampler configurations.

**Tech Stack:** Rust, wgpu 24.0.0 (as indicated by root Cargo.toml, but the task mentions wgpu 29 API - likely referring to the specific version upgrade transition).

---

### Task 1: Update Sampler Mipmap Filter Modes

**Files:**
- Modify: `crates/vorce-render/src/color_calibration_renderer.rs`
- Modify: `crates/vorce-render/src/mesh_renderer.rs`
- Modify: `crates/vorce-render/src/quad.rs`
- Modify: `crates/vorce-render/src/oscillator_renderer.rs`
- Modify: `crates/vorce-render/src/edge_blend_renderer.rs`
- Modify: `crates/vorce-render/src/compositor.rs`
- Modify: `crates/vorce-render/src/effect_chain_renderer/mod.rs`

- [ ] **Step 1: Replace MipmapFilterMode with FilterMode**
Update `mipmap_filter: wgpu::MipmapFilterMode::Linear` (or Nearest) to `mipmap_filter: wgpu::FilterMode::Linear` (or Nearest).

### Task 2: Update PipelineLayoutDescriptor

**Files:**
- Modify: `crates/vorce-render/src/color_calibration_renderer.rs`
- Modify: `crates/vorce-render/src/mesh_renderer.rs`
- Modify: `crates/vorce-render/src/quad.rs`
- Modify: `crates/vorce-render/src/oscillator_renderer.rs`
- Modify: `crates/vorce-render/src/edge_blend_renderer.rs`
- Modify: `crates/vorce-render/src/compositor.rs`
- Modify: `crates/vorce-render/src/shader_graph_integration.rs`
- Modify: `crates/vorce-render/src/effect_chain_renderer/pipeline.rs`

- [ ] **Step 1: Apply descriptor changes**
- Remove `immediate_size: 0`.
- Add `push_constant_ranges: &[]`.
- Change `bind_group_layouts` from `&[Some(&...)]` to `&[&...]`.

### Task 3: Update RenderPipelineDescriptor and RenderPassDescriptor

**Files:**
- All files mentioned in Task 2.
- Modify: `crates/vorce-render/src/effect_chain_renderer/apply.rs`

- [ ] **Step 1: Update multiview field**
Change `multiview_mask: None` to `multiview: None`.

- [ ] **Step 2: Remove depth_slice from RenderPassColorAttachment**
Remove `depth_slice: None` (or any value) from `RenderPassColorAttachment`.

### Task 4: Verification

- [ ] **Step 1: Run cargo check**
Run `cargo check -p vorce-render` to verify all errors are resolved.
