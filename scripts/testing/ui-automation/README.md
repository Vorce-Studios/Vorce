# Vorce UI Automation

This folder contains scripts for testing the Vorce UI programmatically.

## Dependencies

- Python 3
- `pyautogui` for mouse/keyboard automation
- `pillow` (PIL) for screenshot capturing and verification
- `psutil` for process management

Install dependencies via:
```bash
pip install pyautogui pillow psutil
```
