## YYYY-MM-DD - [unwrap() eradication in core dependencies] **Schwachstelle:** [Multiple occurrences of unwrap() and expect() scattered across vorce-core, vorce-render, vorce-bevy, egui_node_editor which could lead to panics] **Lektion:** [We need to always use safe unwrapping using Result, Option matching, and unwrap_or_else to avoid crashes from edge cases] **Prävention:** [Consistently use ? operator, unwrap_or_default, or unwrap_or_else with descriptive panics or fallbacks during initialization]
