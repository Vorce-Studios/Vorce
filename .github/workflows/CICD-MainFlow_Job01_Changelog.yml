name: "CICD-MainFlow: Job01 Changelog & Cleanup"

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  changelog-and-cleanup:
    name: Update Changelog
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Update Changelog (placeholder)
        run: |
          echo "Updating Changelog..."
          # git-cliff --config cliff.toml --output CHANGELOG.md

      - name: Skip legacy issue auto-close logic
        run: |
          echo "Legacy issue auto-close disabled."
          echo "Issue closure must be driven by the merged PR relation or explicit project-manager sync."
