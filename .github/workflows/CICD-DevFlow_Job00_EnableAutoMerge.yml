name: "CICD-DevFlow: Job00 Enable Auto-Merge (disabled)"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: "Disabled"
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled state
        run: |
          echo "Dieser Workflow ist absichtlich deaktiviert."
          echo "Auto-Merge wird zentral über CICD-DevFlow: Job02 Auto-Merge gesteuert."
